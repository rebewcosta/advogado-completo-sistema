
// This component is now removed and replaced by the AdminPage and CriarContaEspecial component
// The file remains but is not used in the application anymore

import React from 'react';

const CreateUserAccount: React.FC = () => {
  // This component is deprecated and no longer used
  return null;
};

export default CreateUserAccount;
